{
  "hide-output-from-background-processes": {
    "default": false,
    "fromEnv": "",
    "switch": true,
    "type": "boolean",
    "hint": ""
  },
  "headless": {
    "default": false,
    "fromEnv": "",
    "switch": true,
    "type": "boolean",
    "hint": "Starts a web server instead of the GUI."
  },
  "headless-server-port": {
    "default": 6275,
    "fromEnv": "",
    "type": "int",
    "switch": false,
    "hint": "Port for the web server, if started in headless mode."
  },
  "update-debs": {
    "default": false,
    "fromEnv": "",
    "type": "boolean",
    "switch": true,
    "hint": "Update Debian packages."
  },
  "update-debs-prerelease": {
    "default": false,
    "fromEnv": "",
    "type": "boolean",
    "switch": true,
    "hint": "Update Debian packages from the pre-release branch"
  },
  "ros-version": {
    "default": "",
    "fromEnv": "ROS_VERSION",
    "type": "string",
    "switch": false,
    "hint": "Set ROS version {1, 2}. Sourced from ROS_VERSION by default."
  },
  "ros-domain-id": {
    "default": -1,
    "fromEnv": "ROS_DOMAIN_ID",
    "type": "int",
    "switch": false,
    "hint": "Set ROS domain id. Sourced from ROS_DOMAIN_ID by default."
  },
  "host": {
    "default": "",
    "fromEnv": "",
    "type": "string",
    "switch": false,
    "hint": "If --start is active, the hosts (separated by commas) can be specified here."
  },
  "rmw-implementation": {
    "default": "",
    "fromEnv": "",
    "type": "string",
    "switch": false,
    "hint": "Override RMW_IMPLEMENTATION."
  },
  "join": {
    "default": false,
    "fromEnv": "",
    "type": "boolean",
    "switch": true,
    "hint": "Connects on startup to the current ROS domain ID and localhost, or to specified host(s)"
  },
  "start": {
    "default": false,
    "fromEnv": "",
    "type": "boolean",
    "switch": true,
    "hint": "Starts the ROS-MAS nodes"
  },
  "join-ws": {
    "default": "",
    "fromEnv": "",
    "type": "string",
    "switch": false,
    "hint": "Daemons to connect to on startup. Format: comma-separated list of HOST:PORT. Example: localhost:12345"
  },
  "help": {
    "default": false,
    "fromEnv": "",
    "type": "boolean",
    "switch": true,
    "hint": "Prints this help and exit"
  }
}
